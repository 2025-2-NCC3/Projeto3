<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.pi">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Cantina"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- APENAS as activities que EXISTEM -->
        <activity
            android:name=".activities.OrderMainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activities.OrderActivity"
            android:exported="false" />

        <!-- Se OrderDetailActivity existir, deixe. Se não, COMENTE ou REMOVA -->
        <activity
            android:name=".activities.OrderDetailActivity"
            android:exported="false" />

    </application>

</manifest>